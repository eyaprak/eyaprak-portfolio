import { FiDatabase } from 'react-icons/fi';
import { IconText } from '../styles/Icon.styled';
function LocalStorage() {
  return (
    <>
      <FiDatabase size={35} color={'green'} />
      <IconText>Local Storage</IconText>
    </>
  );
}

export default LocalStorage;
